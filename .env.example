# ===========================================
# N8N Backup & Restore Tool Configuration
# N8N 備份還原工具配置
# ===========================================

# N8N Configuration - Default (Localhost)
# N8N 配置 - 預設（本地主機）
# Local N8N instance configuration / 本地 N8N 實例的配置
N8N_BASE_URL=http://localhost:5678
N8N_API_KEY=your_n8n_api_key_here

# N8N Environment A (e.g., Zeabur)
# N8N 環境 A（例如：Zeabur）
# First remote N8N environment configuration / 第一個遠端 N8N 環境配置
N8N_A_NAME=Zeabur N8N
N8N_A_BASE_URL=https://your-zeabur-n8n.zeabur.app
N8N_A_API_KEY=your_zeabur_n8n_api_key

# N8N Environment B (e.g., HuggingFace)
# N8N 環境 B（例如：HuggingFace）
# Second remote N8N environment configuration / 第二個遠端 N8N 環境配置
N8N_B_NAME=HuggingFace N8N
N8N_B_BASE_URL=https://your-huggingface-n8n.hf.space/
N8N_B_API_KEY=your_huggingface_n8n_api_key

# N8N Environment C (e.g., Staging)
# N8N 環境 C（例如：測試環境）
# Third remote N8N environment configuration / 第三個遠端 N8N 環境配置
N8N_C_NAME=Staging N8N
N8N_C_BASE_URL=https://your-staging-n8n.com
N8N_C_API_KEY=your_staging_n8n_api_key

# ===========================================
# GitHub Configuration / GitHub 配置
# ===========================================
# GitHub Personal Access Token (requires repo permissions)
# GitHub 個人存取權杖（需要 repo 權限）
GITHUB_TOKEN=ghp_your_github_personal_access_token_here
# GitHub username / GitHub 用戶名
GITHUB_REPO_OWNER=your_github_username
# Repository name for storing backups / 用於存儲備份的 GitHub 儲存庫名稱
GITHUB_REPO_NAME=n8n-backups

# ===========================================
# Google Drive Configuration / Google Drive 配置
# ===========================================
# OAuth 2.0 Client ID created in Google Cloud Console
# 在 Google Cloud Console 中建立的 OAuth 2.0 客戶端 ID
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
# Client secret from Google Cloud Console
# Google Cloud Console 中的客戶端密鑰
GOOGLE_CLIENT_SECRET=GOCSPX-your_google_client_secret
# OAuth redirect URI (must be configured in Google Cloud Console)
# OAuth 重新導向 URI（需要在 Google Cloud Console 中配置）
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback
# Google Drive API refresh token
# Google Drive API 重新整理權杖
GOOGLE_REFRESH_TOKEN=1//your_google_refresh_token_here

# ===========================================
# Security Configuration / 安全配置
# ===========================================
# JWT signing key (recommended: 64-character random string)
# JWT 簽名金鑰（建議使用 64 字元的隨機字串）
JWT_SECRET=your_64_character_jwt_secret_key_here
# Data encryption key (must be 32 characters)
# 資料加密金鑰（必須是 32 字元）
ENCRYPTION_KEY=your_32_character_encryption_key_here

# ===========================================
# Server Configuration / 伺服器配置
# ===========================================
# Server running port / 伺服器執行連接埠
PORT=3003